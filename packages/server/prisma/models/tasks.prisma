
model Tasks {
    id        Int       @id @default(autoincrement())
    slug     String
    id_page   Int
    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt

    page      Pages     @relation(fields: [id_page], references: [id], onDelete: Cascade)
    p_tasks   p_tasks[]
}